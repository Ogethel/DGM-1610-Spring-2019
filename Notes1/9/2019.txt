Console.WriteLine (Hello, World!);
-Above instruction will tell the C# language to print "Hello World" on screen

-Variable names usually use an uppercase letter for each word, except the first one which is always lowercase.

-Stage
-Commit
-Push

var greeting = "Hello Sam!";
-Line will add "Hello Sam!" as a string variable

var verb = "play";
Console.WriteLine (verb);
-this means that we created a variable named verb that stores the vaule "play".

var verb = "play";
Console.WriteLine ("We are" + verb + "ing");
Output=We are playing
-The + sign adds strings together into a single string.

//Notes from 2/6/19

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mammal : MonoBehaviour 
{

	public string Name;

	public Color SkinColor;
	
	// Use this for initialization
	void Start () {
		//Do work
	}
	
	// Update is called once per frame
	void Update () {
		//Do work
	}
}

//Above is C# code, it is just used as an example of parenting object instances to a child instance.  

//Notes for 2/11/19
Working with Ints Floats and Strings

File scripts are hard coded instances in unity

home work to write code that changes Color and write game variables.